---------------------------------------------------------INSTALLING--------------------------------------------
npm install dotenv
----------------------------------------------------IMPORTING----------------------------------------

require("dotenv").config();                 LAZMI LIKHNA BEFORE USING PORT OR ANY OTHER VARIABLE

--------------------------------------------------------INSIDE .ENV-------------------------------------

.env file banalo, and store data like:
SAVED=1234


-------------------------------------------------------WHEREVER TO USE:-------------------------------------------------

const port = process.env.SAVED || 3000;



